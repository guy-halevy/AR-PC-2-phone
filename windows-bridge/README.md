# Windows bridge status

No integrated Windows bridge executable is built in this checkpoint.

The portable Python packet viewer runs as a diagnostic receiver and changes no Windows input or SteamVR state. The shared C++ math is intended for future native integration. Desktop+ adapter design is documented separately. Native build and input/capture behavior require a Windows SDK/compiler and real desktop-session testing.
